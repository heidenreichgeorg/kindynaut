{
	"version": "2.0.0",
	"tasks": [
		{
			"label":"auto",
			"type":"shell",
			"command":"npm start",
            "options": {  "cwd": "${workspaceFolder}" },
			"runOptions":{"runOn":"folderOpen"}
		}
    ]
}